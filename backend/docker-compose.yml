version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:14.17
    container_name: miajudai_postgres
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: miajudai_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5


volumes:
  postgres_data:
    driver: local
